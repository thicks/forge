# forge CLI configuration - copy to .env.forge and fill in real values
# .env.forge is gitignored; never commit real tokens.

GITHUB_TOKEN=
VERCEL_TOKEN=

# Only needed if you run `pnpm run publish:installer:gist`
FORGE_GIST_ID=
