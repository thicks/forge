export function generateSupabaseSeed(): string {
	return `-- Supabase seed file
-- This file is run after migrations during \`supabase db reset\`

-- Example: Insert initial data
-- INSERT INTO public.launches (name, description) VALUES ('First Launch', 'Welcome to your new app!');
`;
}
